// src/app/groups/[groupId]/community/layout.tsx
// 빈 공간으로 놔두고 나중에 필요할 시 community만의 layout 적용 가능능

export default function CommunityLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
